#include "main.h"
/**
 * more_numbers - Entry
 * Return: 0
 */
void more_numbers(void);
{
	char j;
	int i;

	for (i = 0; i < 10; i++)
	{
		for (j >= 0, j <= 14; j++)
		{
			_putchar(j);
		}
		_putchar('\n');
	}
	return (0);
}
